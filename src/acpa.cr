require "./acpa/protocol.cr"

# The ACPA module provides a supporting implementation based on ACP, or Agent Connection Protocol,
# which defines a (hopeful) standard interface for an editor or IDE to communicate with AI agents.
module ACPA; end
