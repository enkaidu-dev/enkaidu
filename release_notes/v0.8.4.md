
#### FEATURES

* Support configuring built-in tools using settings specified via the profile and app configuration YAML files.
* The `shell_command` tool supports the following settings:
    * `allowed_commands` to specify commands the tool will prompt for confirmation
    * `approved_commands` to specify commands that wont be confirmed
    * The environment variables are still supported if no configuration provided

#### FIXES

> N/a
